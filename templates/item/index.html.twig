{% extends 'base.html.twig' %}

{% block body %}
    {% for item in items %}
        <div class="card"
        style="background-color: {{ item.rarity.color }}">
            <h1>{{ item.name }}</h1>
            <p>{{ item.description }}</p>
        </div>
    {% endfor %}
{% endblock %}
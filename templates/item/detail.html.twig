{% extends 'base.html.twig' %}

{% block title %}Items {% if rarity.name %}- {{ item.name }}{% endif %}{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-body">
            {{ form(form) }}
        </div>
    </div>
{% endblock %}


{% extends 'base.html.twig' %}

{% block body %}
    <div class="container pt-3">
        <div class="section mb-5">
            <div class="row justify-content-center">
                <div class="col-12 col-md-6">
                    <a class="btn btn-primary p-5 w-100" href="{{ path('item_random') }}">LOOOOOOT!</a>
                </div>
            </div>
        </div>
        <div class="section mb-5">
            <div class="row justify-content-center">
                <div class="col-12 col-md-6">
                    <a class="btn btn-primary p-5 w-100" href="{{ path('rarity_index') }}">Zu den Raritäten</a>
                </div>
                <div class="col-12 col-md-6">
                    <a class="btn btn-primary p-5 w-100" href="{{ path('rarity_new') }}">Neue Raritäten</a>
                </div>
            </div>
        </div>
        <div class="section mb-5">
            <div class="row justify-content-center">
                <div class="col-12 col-md-6">
                    <a class="btn btn-primary p-5 w-100" href="{{ path('table_index') }}">Zu den Tabellen</a>
                </div>
                <div class="col-12 col-md-6">
                    <a class="btn btn-primary p-5 w-100" href="{{ path('table_new') }}">Neue Tabelle</a>
                </div>
            </div>
        </div>
        <div class="section mb-5">
            <div class="row justify-content-center">
                <div class="col-12 col-md-6">
                    <a class="btn btn-primary p-5 w-100" href="{{ path('item_index') }}">Zu den Items</a>
                </div>
                <div class="col-12 col-md-6">
                    <a class="btn btn-primary p-5 w-100" href="{{ path('item_new') }}">Neues Item</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Raritäten {% if rarity.name %}- {{ rarity.name }}{% endif %}{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-body">
            {{ form(form) }}
        </div>
    </div>
{% endblock %}


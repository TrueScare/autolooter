{% trans_default_domain 'mails' %}
<h1>{{ 'registration_confirm.title'|trans }}</h1>

<p>
    {{ 'registration_confirm.please_confirm'|trans }}<br><br>
    <a href="{{ signedUrl|raw }}">{{ 'registration_confirm.button_label'|trans }}</a>.
    {{ 'registration_confirm.expires_note'|trans({'%time%': expiresAtMessageKey|trans(expiresAtMessageData, 'VerifyEmailBundle') }) }}
</p>

<p>
    {{ 'registration_confirm.greetings'|trans }}
</p>
